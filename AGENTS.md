# people-graph

**In one line:** Canonical graph linking 339,217 people to the books, repositories and videos they made, with topics and external identities in one SQLite database. District: `Great_Library_of_SISO` (`~/SISO_Workspace/Great_Library_of_SISO/people-graph`).

The rest is in `README.md`. Find anything else on this machine with `estate where <words>`.
